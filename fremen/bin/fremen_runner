#!/usr/bin/env perl

use warnings;
use strict;

use Data::Dumper;

use Fremen;
use Fremen::Runner;

my $runner = Fremen::Runner->new();
$runner->parse_options();

# TODO agent process to launch the runners

#print Dumper($runner);

$runner->start();
